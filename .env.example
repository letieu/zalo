# Omniroute LLM gateway (OpenAI-compatible, k3s-internal)
# Defaults point at the omniroute ClusterIP reachable from this host; no auth needed internally.
OMNIROUTE_BASE_URL=http://10.43.196.168:20128/v1
# OMNIROUTE_API_KEY=
# OMNIROUTE_MODEL=auto/best-fast

# Optional: direct LLM providers (fallbacks when ai_provider is set to them)
# GEMINI_API_KEY=
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
# OPENAI_MODEL=gpt-4o-mini
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=llama3
